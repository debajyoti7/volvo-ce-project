
public class DBSCAN2 {

	/*
	interface DistanceMetrics<T> {
		
		double distance(Vec2D one, Vec2D two);
	}

	class DoubleArrayEuclideanDistanceMetrics implements DistanceMetrics<double[]> {

		@Override
		public double distance(Vec2D one, Vec2D two) {
			double squaredDistance = 0.0;
			for (int i = 0; i < one.data.length; i++) {
				double d = one.data[i] - two.data[i];
				squaredDistance += d * d;
			}
			return Math.sqrt(squaredDistance);
		}
	};
	*/

}
